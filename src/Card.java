//enum color {Red, Green, Yellow, Blue, None}
//enum value {Zero, One, Two, Three, Four, Five, Six, Seven, Eight, Nine, Ten, DrawTwo, Skip, Revers, Wild, WildDrawFour}

public class Card {

    //Make private after getter is written
    int color;
    int value;

    Card(){
        this.color = 0;
        this.color = 1;
    }

    Card(int color, int value){
        this.color = color;
        this.value = value;
        }
    }

